import { useState } from "react";import { createBacktest,STRATEGIES,type RunSummary } from "../api/backtest";
export function BacktestCreateForm({onCreated}:{onCreated:(run:RunSummary)=>void}){const [busy,setBusy]=useState(false),[error,setError]=useState<string|null>(null),[exclude,setExclude]=useState(false);
 async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError(null);const f=new FormData(e.currentTarget);const rate=(name:string)=>String(f.get(name));try{onCreated(await createBacktest({strategyCode:String(f.get("strategy")) as never,symbol:String(f.get("symbol")).trim().toUpperCase(),startDate:String(f.get("start")),endDate:String(f.get("end")),initialCapitalVnd:rate("capital"),riskPerTrancheRate:rate("risk"),maxAggregateOpenRiskRate:rate("heat"),costs:exclude?{excluded:true}:{excluded:false,entryFeeRate:rate("entryFee"),exitFeeRate:rate("exitFee"),sellTaxRate:rate("tax"),entrySlippageRate:rate("entrySlip"),exitSlippageRate:rate("exitSlip")}}))}catch(x){setError(x instanceof Error?x.message:"Không thể tạo backtest")}finally{setBusy(false)}}
 return <form onSubmit={submit} className="panel" aria-label="Tạo backtest"><h2>Chạy backtest mới</h2><label>Chiến lược<select name="strategy">{STRATEGIES.map(x=><option key={x}>{x}</option>)}</select></label><label>Mã cổ phiếu<input name="symbol" pattern="[A-Za-z0-9]{1,10}" required placeholder="FPT"/></label><label>Từ ngày<input name="start" type="date" required/></label><label>Đến ngày đã hoàn tất<input name="end" type="date" required/></label><label>Vốn ban đầu (VND)<input name="capital" inputMode="decimal" defaultValue="100000000" required/></label><label>Risk mỗi tranche<input name="risk" defaultValue="0.01" required/></label><label>Tổng open risk tối đa<input name="heat" defaultValue="0.04" required/></label><label><input type="checkbox" checked={exclude} onChange={e=>setExclude(e.target.checked)}/> Loại chi phí (kết quả sẽ có cảnh báo)</label>{!exclude&&<fieldset><legend>Chi phí theo tỷ lệ thập phân</legend><label>Phí mua<input name="entryFee" defaultValue="0.0015"/></label><label>Phí bán<input name="exitFee" defaultValue="0.0015"/></label><label>Thuế bán<input name="tax" defaultValue="0.001"/></label><label>Trượt giá mua<input name="entrySlip" defaultValue="0.001"/></label><label>Trượt giá bán<input name="exitSlip" defaultValue="0.001"/></label></fieldset>}<button disabled={busy}>{busy?"Đang tạo…":"Chạy backtest"}</button>{error&&<p role="alert">{error}</p>}</form>}
